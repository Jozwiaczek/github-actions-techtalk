{
  "name": "ci-scripts",
  "version": "1.0.0",
  "private": true,
  "keywords": [
    "ci",
    "github-actions"
  ],
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2"
  },
  "engines": {
    "node": ">=16",
    "yarn": ">=1.2"
  }
}
